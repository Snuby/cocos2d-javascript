<!doctype html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
        <title>Cocos2D -- Tests</title>
        <style type="text/css" media="screen">
            body {
                font-family: Helvetica, Arial, sans-serif;
                font-size: 10pt;
                background: #ccc;
            }
            #wrapper {
                position: relative;
                min-height: 320px;
                background: #fff;
                width: 730px;
                padding: 16px;
                overflow: auto;
                margin: auto;
                border: 2px solid #333;
                border-radius: 8px;
                box-shadow: 0px 3px 6px #333;
            }
            #wrapper h1 {
                margin-top: 0;
            }
            .log {
                padding: 2px 8px;
                border-bottom: 1px solid #999;
            }
            .log.pass {
                background: #4d4;
            }
            .log.fail {
                background: #d44;
            }
            .log.info {
                background: #ccc;
            }
            .logs {
                overflow: auto;
            }
            nav {
                float: left;
            }
            nav ul, 
            nav li {
                padding: 0;
                margin: 0;
                list-style: none;
                width: 200px;
            }
            nav a::before {
                content: '•  '
            }
            nav a {
                display: block;
                padding: 2px 8px;
                text-decoration: none;
            }
            nav a:hover {
                background: #ddd;
            }
            #${containerID} {
                width: 480px;
                height: 320px;
                position: absolute;
                top: 16px;
                right: 16px;
                box-shadow: 0px 3px 6px #333;
            }
        </style>
    </head>
    <body>
        ${scripts}
        <div id="wrapper">
            <h1>Cocos2D Tests</h1>
            <p>Select a test suite to run:</p>
            <nav>
                <ul>
                    <li><a href="?test=init_commonjs">CommonJS</a></li>
                    <li><a href="?test=cocos2d/SpriteTest">Sprites</a></li>
                    <li><a href="?test=cocos2d/ActionTest">Actions</a></li>
                    <li><a href="?test=cocos2d/EaseActionTest">Ease Actions</a></li>
                    <li><a href="?test=cocos2d/LabelTest">Labels</a></li>
                    <li><a href="?test=cocos2d/TileMapTest">Tile Maps</a></li>
                    <li><a href="?test=cocos2d/TransitionTest">Scene Transitions</a></li>
                </ul>
            </nav>
            <div id="${containerID}">
                 <h2 id="qunit-banner"></h2>
                 <div id="qunit-testrunner-toolbar"></div>
                 <h2 id="qunit-userAgent"></h2>
                 <ol id="qunit-tests"></ol>
                 <div id="qunit-fixture">test markup, will be hidden</div>
            </div>
        </div>
    </body>
</html>
